<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aditia">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/burger"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/burger"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".CreateRestaurantActivity" android:label="@string/create_resto"/>
        <activity android:name=".ProfileActivity" android:label="@string/app_profile"/>
        <activity android:name=".ChangePasswordActivity" android:label="@string/edit_profil"/>
        <activity android:name=".DashboardActivity" android:label="@string/app_name"/>/>
        <activity android:name=".auth.RegisterActivity" android:label="@string/register"/>/>
        <activity android:name=".auth.LoginActivity" android:label="@string/login"/>/>
        <activity android:name=".SplashScreenActivity" android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>